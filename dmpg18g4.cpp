#include <bits/stdc++.h>
using namespace std;

#define MAXN 2001

int v[MAXN];

int main(){
    int n,a,b;scanf("%d%d%d",&n,&a,&b);
    for(int i=0;i<n;i++)
        scanf("%d", &v[i]);
    sort(v, v+n);

}
