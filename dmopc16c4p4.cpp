#include <iostream>
#include <unordered_set>

using namespace std;

#define MAXN 100001
#define MAXBS 330

int series[MAXN];


int main() {
	int n;
	scanf("%d", &n);
	for (int i = 0; i < n; i++) 
		scanf("%d", &series[i]);

	int q;
	scanf("%d", &q);
	for (int i = 0; i < q; i++) {

	}
}