#include <bits/stdc++.h>
using namespace std;

typedef pair<int,int> pii;
typedef pair<double,pii> pdi;
typedef long long ll;
#define f first
#define s second

#define MAXN 50

int a[MAXN], b[MAXN];

int main(){
    int n;scanf("%d",&n);
    int bloc=n/2;
    for(int i=0;i<n;i++)
        scanf("%d", &a[i]);
    for(int j=0;j<n;j++)
        scanf("%d", )
}
